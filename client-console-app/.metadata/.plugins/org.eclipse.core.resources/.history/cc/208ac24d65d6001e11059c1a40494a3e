package command;

import account.UserAccount;
import service.UserAccountService;

public class AvailableBalanceCommand implements Command {

	private final UserAccountService accountService;
	
	public AvailableBalanceCommand(UserAccountService accountService) {
		this.accountService = accountService;
	}
	
	@Override
	public void execute(UserAccount account) {
		double balance = accountService.getBalance(Integer.valueOf(account.getCardNumber());
		
	}

}
